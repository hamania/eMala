{
  "name": "emala-frontend",
  "version": "1.0.0",
  "description": "Vue.js frontend with 9 clickable buttons",
  "main": "src/main.js",
  "scripts": {
    "dev": "npx vite --port 3001 --host",
    "build": "npx vite build",
    "preview": "npx vite preview --port 3001 --host"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}